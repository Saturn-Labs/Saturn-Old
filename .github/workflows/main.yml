name: C/C++ Premake Saturn Compilation
run-name: ${{ github.actor }}
on: [push]
jobs:
  COMPILE-WINDOWS:
    runs-on: windows-latest
    steps: 
      - run: ${{ github.workspace }}/../generate_projects.bat
